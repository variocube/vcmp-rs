# The Rust harness parses stdout line by line, so keep Spring's own logging on stderr-ish levels
# and out of the way. Port and heartbeat are supplied by the harness via system properties.
spring.main.banner-mode=off
logging.level.root=WARN
logging.level.com.variocube.vcmp=INFO
server.port=${server.port:2000}
